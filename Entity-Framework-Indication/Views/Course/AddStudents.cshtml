@model SchoolDbContext
@{
    ViewData["Title"] = "Edit";
}
<hr />
<h1>Add Student to course</h1>
<br />
<hr />

<table>
    <thead>

         @foreach (var item in Model.Sc)
         {
             <partial name="_StudentsCoursesTesting" model="item" />
         }
        @*@if (Model.Courses != null)
            {
                foreach (var value in Model.Courses)
                {
            <tr>
                <th>Chosen course</th>
                <td>@value.Title</td>
                <td>@value.Subject</td>
                @if (value.Teacher != null)
                {
                    <td>@value.Teacher</td>
                }
                @if (value.Assignments != null)
                {
                    foreach (var item in value.Assignments)
                    {
                        <td>@item.Title</td>
                        <td>@item.Subject</td>
                        <td>@item.Description</td>
                        <td>@item.DueToDate</td>
                    }
                }
                @if (Model.Students != null)
                {
                    foreach (var item in Model.Students)
                    {
                        <td>@item.FirstName</td>
                        <td>@item.SecondName</td>
                        <td>@item.SchoolYear</td>
                        <td>@item.PhoneNumber</td>
                    }

                }
            </tr>
                }
            }*@
        @*@foreach (var item in Model.Students)
            {
            <partial name="_AddStudents" model="item" />
            }*@


    </thead>
    <tbody>
        <tr>
            @if (Model.Students != null)
            {
                foreach (var item in Model.Students)
                {
                    <td>
                        <partial name="_AddStudentsStudentTest" model="item" />
                    </td>
                }
            }
        </tr>
        <tr>
            <td>
                <a class="btn btn-success" asp-action="Index" asp-controller="Course">Return</a>
            </td>
        </tr>
    </tbody>
</table>